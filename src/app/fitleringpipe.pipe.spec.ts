import { FitleringpipePipe } from './fitleringpipe.pipe';

describe('FitleringpipePipe', () => {
  it('create an instance', () => {
    const pipe = new FitleringpipePipe();
    expect(pipe).toBeTruthy();
  });
});
